<h1>Users#show</h1>
<p>User's email: <%= current_user.email %></p>

<div class="col-md-8">
 <%= render partial: 'items/form', locals: { item: @item } %>
</div>

<div class="col-md-8">
 <%= render partial: 'items/item', locals: { item: @item } %>
</div>
